
export enum Paths {
    MAIN = "/",
    LOGIN = "/login",
    FORGOT_PASSWORD = "/forgot-password",
    SET_PASSWORD = "/set-password"
}

export enum localStorageItems {
    ACCESS_TOKEN = "access_token",
    REFRESH_TOKEN = "refresh_token"
}

export const access_id = "test123"
export const secret = "secret123"